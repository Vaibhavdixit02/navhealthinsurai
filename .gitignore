.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.log

# -----------------------------------------------------------------------------
# Raw data sources (exports, emails, chats, third-party PDFs) — local only, not in git.
# Other PDFs in the tree can be committed normally when you add them.
# -----------------------------------------------------------------------------

Chat.eml
WhatsApp Chat - Manav Sevak/

# Gmail PDF exports + local copy of seed memo (when in repo root)
Gmail*.pdf
V1 Memo.pdf

# Optional: drop ad-hoc raw files here; entire folder stays untracked
data-sources/
